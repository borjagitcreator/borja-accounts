[project]
name = "borja-accounts"
version = "0.1.0"
description = "Panel de control financiero personal — cuentas y cartera de inversión"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1,<4.0",
    "pandas>=2.3,<3.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
